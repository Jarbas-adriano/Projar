CREATE OR REPLACE TRIGGER TGCONTAS_RECEBER
BEFORE INSERT OR UPDATE OR DELETE ON TBCONTAS_RECEBER FOR EACH ROW
DECLARE
BEGIN
   IF INSERTING THEN
   NULL;
    --  :NEW.VALOR_REAL_RECEBIDO := :NEW.TOTAL_PARCELA;
   END IF;
END;
/
